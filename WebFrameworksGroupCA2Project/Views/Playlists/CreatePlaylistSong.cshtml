@model WebFrameworksGroupCA2Project.Models.PlaylistSong

@{
    ViewData["Title"] = "CreatePlaylistSong";
}

<h1>Create</h1>

<h4>PlaylistSong</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreatePlaylistSong">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PlaylistId" />
            <div class="form-group">
                <label asp-for="SongId" class="control-label"></label>
                <select style="font-weight: bold; color: black;" asp-for="SongId" class="form-control" asp-items="ViewBag.SongId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}